

describe('Roost SVC URL', () => {
  it('should return a status code of 201 when making a GET call to the "/" endpoint', async () => {
    const response = await fetch(`${ROOST_SVC_URL}/`);
    expect(response.status).toBe(201);
  });
});